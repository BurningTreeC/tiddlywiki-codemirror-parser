import resolve from "@rollup/plugin-node-resolve";
import commonjs from "@rollup/plugin-commonjs";
import terser from "@rollup/plugin-terser";

function twBanner(title) {
  return `/*\\
title: ${title}
type: application/javascript
module-type: library

Bundled CodeMirror 6 keymap module.
DO NOT EDIT - generated by Rollup.

\\*/
/*jslint node: true, browser: true */
"use strict";
`;
}

// Paths to core libraries (these are external dependencies)
const twPaths = {
  "@codemirror/state":
    "$:/plugins/BurningTreeC/tiddlywiki-codemirror/lib/codemirror-state.js",
  "@codemirror/view":
    "$:/plugins/BurningTreeC/tiddlywiki-codemirror/lib/codemirror-view.js",
  "@codemirror/commands":
    "$:/plugins/BurningTreeC/tiddlywiki-codemirror/lib/codemirror-commands.js",
  "@codemirror/language":
    "$:/plugins/BurningTreeC/tiddlywiki-codemirror/lib/codemirror-language.js",
  "@codemirror/autocomplete":
    "$:/plugins/BurningTreeC/tiddlywiki-codemirror/lib/codemirror-autocomplete.js",
  "@codemirror/search":
    "$:/plugins/BurningTreeC/tiddlywiki-codemirror/lib/codemirror-search.js",
  "@lezer/common":
    "$:/plugins/BurningTreeC/tiddlywiki-codemirror/lib/lezer-common.js",
  "@lezer/highlight":
    "$:/plugins/BurningTreeC/tiddlywiki-codemirror/lib/lezer-highlight.js"
};

// Keymap plugins to build
const keymaps = [
  {
    name: "vim",
    input: "@replit/codemirror-vim",
    output: "plugins/keymap-vim/codemirror-vim.js",
    title: "$:/plugins/BurningTreeC/tiddlywiki-codemirror/plugins/keymap-vim/codemirror-vim.js"
  }
];

// All external dependencies
const externals = Object.keys(twPaths);

export default keymaps.map((keymap) => ({
  input: keymap.input,
  external: externals,
  output: {
    file: keymap.output,
    format: "cjs",
    exports: "named",
    banner: twBanner(keymap.title),
    paths: twPaths,
    esModule: false
  },
  plugins: [
    resolve({ browser: true }),
    commonjs(),
    terser({
      format: {
        comments: function(node, comment) {
          // Keep TiddlyWiki metadata comments (/*\ ... \*/)
          return comment.type === "comment2" && /^\\/.test(comment.value);
        }
      }
    })
  ]
}));
